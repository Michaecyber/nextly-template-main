import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { ThemeProvider } from "next-themes";
import "./globals.css";

import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";
import { PopupWidget }  from "@/components/PopupWidget";


const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Jiet-edu",
  description: "Johannesburg Institute of Engineering and Technology College. We offers a comprehensive array of educational opportunities across a broad spectrum of fields. From engineering and management to media and IT, you cover a wide range of industries, catering to diverse interests and career aspirations. The availability of full-time, part-time, and online classes reflects a commitment to accommodating different schedules and learning preferences. ",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class">
          <Navbar />
          <div>{children}</div>
          <Footer />
          <PopupWidget />
        </ThemeProvider>
      </body>
    </html>
  );
}
